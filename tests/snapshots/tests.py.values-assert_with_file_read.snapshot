Oops! Assertion failed

The following assertion was not satisfied
  assert not that(fp.read()), error()

Assertion values
  assert not that(fp.read()), error()
             ^ is = 'hello'
  assert not that(fp.read()), error()
         ^ is = False
  assert not that(fp.read()), error()
  ^ is False